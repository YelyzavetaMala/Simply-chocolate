const burger = document.querySelector('.header-button');
const modal_button_close = document.querySelector('.modal-button-close');
const header_modal = document.getElementById('header-modal');

burger.addEventListener('click', () => {
  header - modal.showModal();
});

modal -
  button -
  close.addEventListener('click', () => {
    header - modal.close();
  });
